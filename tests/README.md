# Tests Directory

This directory stores cross-module validation assets that do not belong to a single runtime package, such as:
- smoke test checklists
- API contract samples
- scenario test data
- manual verification notes
