# 评测与验证说明

## 一、文档目的
记录本轮“学生考试态修复、独立建卷页重构、AI 题库辅助接入”的真实验证结果。

## 二、构建与自动化验证
- 后端 `mvn -q test`：通过
- 后端 `mvn -q -DskipTests compile`：通过
- 前端 `npm.cmd run build`：通过
- 前端 `npm.cmd run test:e2e`：通过（5 / 5）

## 三、浏览器级验证
### 1. 学生端考试态
- 进入考试：通过
- 答题卡固定：通过
- 进入全屏 -> 退出全屏：通过
- 主动退出全屏不记异常：通过
- 切换标签 / 页面不可见记异常：通过
- 保存答案：通过
- 提交确认弹窗：通过

### 2. 管理端建卷流程
- 进入试卷管理列表页：通过
- 从列表页进入独立建卷页：通过
- 手工选题建卷：通过
- 随机组卷建卷：通过
- 策略组卷建卷：通过
- 卷面预览与保存：通过

### 3. AI 题库辅助
- 题库页“AI 生成题目草稿”入口：通过
- 题目编辑弹窗“AI 优化当前题目”入口：通过
- 当前环境未配置密钥时，前端反馈中文错误：通过
- 后端 `/api/exam/questions/ai/draft` 返回中文错误：通过

## 四、数据库与接口验证
- `exam_system` 数据库存在：通过
- 当前新增自动化试卷：
  - `MAN-*`
  - `RND-*`
  - `STR-*`
- 当前新增自动化考试：
  - `STD-*`
  - `UI-*`
  - `GRD-*`
  - `AUTO-KS-*`
- 防作弊事件验证：
  - 主动退出全屏前后 `FULLSCREEN_EXIT` 不增加
  - 切换标签后 `TAB_SWITCH` 增加

## 五、当前 blocker
- 当前机器未设置 `AI_API_KEY`
- 因此 AI 模块已验证到“配置缺失时的明确反馈”，未完成真实外部成功调用
